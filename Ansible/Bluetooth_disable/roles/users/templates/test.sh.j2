#!/bin/bash

#if [ `grep  -a  "rfkill block bluetooth" ./rc.local` ]
if [[ `cat "/etc/rc.local" | grep "rfkill block bluetooth"` ]];
then
   echo "YES"
   exit 0
else
   echo "NO"
   sed -i 's/exit 0/rfkill block bluetooth/' /etc/rc.local
   echo "exit 0" >> /etc/rc.local

fi

echo $?


#sed -i 's/exit 0/rfkill block bluetooth/' ./rc.local
#echo "exit 0" >> rc.local
